version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: regulation_monitoring
    container_name: regulation_monitoring
    ports:
      - "5048:5048"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
    volumes:
      - ./data:/app/data